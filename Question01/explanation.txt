Command:
chmod +x analyze.sh

Explanation:
This command gives execute permission to the shell script so that it can be run from the terminal.

Command:
./analyze.sh

Explanation:
This command runs the shell script. Since no logic has been added yet, the script executes without producing any output.
Command:
chmod +x analyze.sh

Explanation:
This command gives execute permission to the script so it can be run from the terminal.

Command:
./analyze.sh

Explanation:
This command runs the script without providing any arguments. The script correctly detects the missing argument and displays an error message.

